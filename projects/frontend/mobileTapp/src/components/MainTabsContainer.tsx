import BottomNavigation from '@/mobileTapp/components/Navigation/BottomNavigation';
import React from 'react';

// import { usePartnershipState } from '@/fsa/store/getters'

const MainTabsContainer = () => {
  // const { partnershipName: header } = usePartnershipState()

  return <BottomNavigation header={''} />;
};

export default MainTabsContainer;
